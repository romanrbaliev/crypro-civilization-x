
import React from 'react';
import { useGame } from '@/context/hooks/useGame';
import { Button } from '@/components/ui/button';
import { debugKnowledgeProduction } from '@/utils/debugCalculator';
import { useEffect, useState } from 'react';

const DebugCalculator = () => {
  // Компонент устарел и заменен на KnowledgeProductionPopup
  // Возвращаем null, чтобы компонент не отображался
  return null;
};

export default DebugCalculator;
